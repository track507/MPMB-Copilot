[project]
name = "mpmb-repo-analyzer"
version = "0.1.0"
description = "Standalone analyzer for MPMB source repositories"
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[tool.uv]
package = false

