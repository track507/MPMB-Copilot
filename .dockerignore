# ==============================================================================
# .dockerignore for MPMB-Copilot Backend Build
# Build context: Project root (.)
# ==============================================================================

# Python virtual environments - CRITICAL TO EXCLUDE
backend/.venv
backend/.venv/
backend/venv
backend/venv/
.venv/
venv/

# Python bytecode and cache
**/__pycache__
**/__pycache__/
**/*.pyc
**/*.pyo
**/*.pyd
**/.pytest_cache
**/.pytest_cache/
**/.mypy_cache
**/.mypy_cache/
**/.ruff_cache
**/.ruff_cache/

# Testing and coverage
backend/.coverage
backend/htmlcov
backend/htmlcov/
backend/tests
backend/tests/
**/htmlcov/
**/.coverage
**/.tox/

# Development files that don't need to be in image
backend/.python-version
backend/README.md
**/*.md
!data/README.md

# IDE and editor files
.vscode/
.idea/
**/.vscode/
**/.idea/
**/*.swp
**/*.swo
**/*~

# Git
.git/
.gitignore
.gitattributes
**/.git/

# Environment files
.env
.env.*
**/.env
**/.env.*

# Docker files (don't need these inside the image)
Dockerfile
docker-compose.yml
docker-compose.*.yml
**/.dockerignore

# OS files
.DS_Store
Thumbs.db
**/.DS_Store
**/Thumbs.db

# Data directories that are large or generated at runtime
# All runtime data is provided via volume mounts in docker-compose.yml
data/*
!data/intent_examples.json
!data/settings.json
!data/README.md

# Scripts directory (except what's needed for postgres init)
scripts/chunk_mpmb.py
scripts/*.ps1

# Build artifacts
**/dist/
**/build/
**/*.egg-info/

# Logs (keep directory structure, exclude log files)
**/logs/*.log
**/logs/*.txt

# * Node modules are rebuilt inside the image (pnpm's host node_modules are symlink farms)
**/node_modules

# * Local caches and generated reports - context bloat only, nothing in the image needs them
.uv-cache
scripts/analyze/reports
