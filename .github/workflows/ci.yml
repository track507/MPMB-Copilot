name: CI

on:
    push:
        branches: [main, develop]
    pull_request:
        branches: [main, develop]
    workflow_dispatch:

permissions:
    contents: read

concurrency:
    group: ci-${{ github.workflow }}-${{ github.ref }}
    cancel-in-progress: true

env:
    HUSKY: "0"
    UV_CACHE_DIR: .uv-cache
    FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"

jobs:
    quality:
        name: Format, Lint, and Test
        runs-on: ubuntu-24.04
        timeout-minutes: 30
        services:
            postgres:
                image: postgres:16-alpine
                env:
                    POSTGRES_USER: mpmb_user
                    POSTGRES_PASSWORD: mpmb_password
                    POSTGRES_DB: mpmb_test
                ports:
                    - 5432:5432
                options: >-
                    --health-cmd pg_isready
                    --health-interval 10s
                    --health-timeout 5s
                    --health-retries 5

        env:
            TEST_DATABASE_URL: postgresql+asyncpg://mpmb_user:mpmb_password@127.0.0.1:5432/mpmb_test

        steps:
            - name: Checkout
              uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

            - name: Setup pnpm
              uses: pnpm/action-setup@ea17c68df8912ef543352723c149a84f56e3d413 # v6.1.0

            - name: Setup Node.js
              uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
              with:
                  node-version-file: ".nvmrc"
                  cache: pnpm

            - name: Setup Python
              uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
              with:
                  python-version: "3.13"

            - name: Setup uv
              uses: astral-sh/setup-uv@bec219d24cd3e171d82865faccec33120bb574f4 # v10.1.0
              with:
                  enable-cache: true
                  cache-dependency-glob: backend/uv.lock

            - name: Install Node dependencies
              run: pnpm install --frozen-lockfile

            - name: Sync backend dependencies
              run: uv sync --project backend --group dev --frozen

            - name: Run repository checks
              run: pnpm run check

    setup-linux:
        name: Setup Script (Linux)
        runs-on: ubuntu-24.04
        timeout-minutes: 30

        steps:
            - name: Checkout
              uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

            - name: Setup pnpm
              uses: pnpm/action-setup@ea17c68df8912ef543352723c149a84f56e3d413 # v6.1.0

            - name: Setup Node.js
              uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
              with:
                  node-version-file: ".nvmrc"
                  cache: pnpm

            - name: Setup Python
              uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
              with:
                  python-version: "3.13"

            - name: Setup uv
              uses: astral-sh/setup-uv@bec219d24cd3e171d82865faccec33120bb574f4 # v10.1.0
              with:
                  enable-cache: true
                  cache-dependency-glob: backend/uv.lock

            - name: Install Node dependencies
              run: pnpm install --frozen-lockfile

            - name: Run setup
              run: node scripts/setup.mjs

            - name: Assert setup produced a usable tree
              shell: bash
              run: |
                  shopt -s nullglob
                  fail=0

                  [[ -f .env ]] || { echo "::error::setup did not create .env from .env.example"; fail=1; }
                  [[ -d backend/.venv ]] || { echo "::error::setup did not create backend/.venv"; fail=1; }
                  [[ -f scripts/analyze/reports/mpmb-analysis.json ]] || { echo "::error::analyzer report missing"; fail=1; }

                  chunks=(data/chunked_output/*.json)
                  if (( ${#chunks[@]} == 0 )); then
                      echo "::error::chunker produced no JSON output"
                      fail=1
                  else
                      echo "Chunk files: ${chunks[*]}"
                  fi

                  for repo in data/mpmb_source data/mpmb_source_2024 data/imports_source; do
                      [[ -d "$repo/.git" ]] || { echo "::error::$repo was not cloned"; fail=1; }
                  done

                  exit "$fail"

    typecheck:
        name: Typecheck
        runs-on: ubuntu-24.04
        timeout-minutes: 30
        if: vars.ENABLE_TYPECHECK == 'true'
        continue-on-error: true

        steps:
            - name: Checkout
              uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

            - name: Setup pnpm
              uses: pnpm/action-setup@ea17c68df8912ef543352723c149a84f56e3d413 # v6.1.0

            - name: Setup Node.js
              uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
              with:
                  node-version-file: ".nvmrc"
                  cache: pnpm

            - name: Setup Python
              uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
              with:
                  python-version: "3.13"

            - name: Setup uv
              uses: astral-sh/setup-uv@bec219d24cd3e171d82865faccec33120bb574f4 # v10.1.0
              with:
                  enable-cache: true
                  cache-dependency-glob: backend/uv.lock

            - name: Install Node dependencies
              run: pnpm install --frozen-lockfile

            - name: Sync backend dependencies
              run: uv sync --project backend --group dev --frozen

            - name: Run type checks
              run: pnpm run typecheck

    frontend-build:
        name: Frontend Production Build
        runs-on: ubuntu-24.04
        timeout-minutes: 20

        steps:
            - name: Checkout
              uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

            - name: Setup pnpm
              uses: pnpm/action-setup@ea17c68df8912ef543352723c149a84f56e3d413 # v6.1.0

            - name: Setup Node.js
              uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
              with:
                  node-version-file: ".nvmrc"
                  cache: pnpm

            - name: Install Node dependencies
              run: pnpm install --frozen-lockfile

            - name: Build frontend
              run: pnpm run frontend:build

            - name: Upload frontend dist artifact
              uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
              with:
                  name: frontend-dist
                  path: frontend/dist/
                  if-no-files-found: error
                  retention-days: 14

    docker-build:
        name: Backend Docker Build
        runs-on: ubuntu-24.04
        timeout-minutes: 30

        steps:
            - name: Checkout
              uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

            - name: Create CI env file for Compose validation
              run: cp .env.example .env

            - name: Validate Compose file
              run: docker compose config --quiet

            - name: Build backend image
              run: docker build -f docker/backend/Dockerfile -t mpmb-copilot-backend:${{ github.sha }} .

    analyzer-smoke:
        name: Standalone Analyzer Smoke Test
        runs-on: ubuntu-24.04
        timeout-minutes: 15

        steps:
            - name: Checkout
              uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

            - name: Setup Python
              uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
              with:
                  python-version: "3.13"

            - name: Check analyzer presence
              id: analyzer
              shell: bash
              run: |
                  if [[ -f scripts/analyze/analyze-repos.py && -f scripts/analyze/src/mpmb_repo_analyzer/__main__.py ]]; then
                      echo "present=true" >> "$GITHUB_OUTPUT"
                  else
                      echo "present=false" >> "$GITHUB_OUTPUT"
                      echo "Analyzer files are not present in this commit; skipping smoke test."
                  fi

            - name: Compile analyzer package
              if: steps.analyzer.outputs.present == 'true'
              run: python -m py_compile scripts/analyze/analyze-repos.py scripts/analyze/src/mpmb_repo_analyzer/__main__.py
